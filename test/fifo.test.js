import test from'node:test';import assert from'node:assert/strict';import{StoreMesh}from'../src/domain.js';test('cold storage enforces FIFO for the same product',()=>{let tick=0;const app=new StoreMesh({clock:()=>new Date(1700000000000+tick++*1000).toISOString()}),s=app.openSession('op','TEST-DEVICE'),ca=app.createContainer({capacityKg:5},'container-a'),cb=app.createContainer({capacityKg:5},'container-b'),a=app.receive({sessionId:s.id,containerId:ca.id,supplier:'S',product:'T',grade:'A',size:'L',weightKg:2},'a'),b=app.receive({sessionId:s.id,containerId:cb.id,supplier:'S',product:'T',grade:'B',size:'M',weightKg:2},'b');app.move(a.id,'COLD_ROOM',s.id,'ca');app.move(b.id,'COLD_ROOM',s.id,'cb');assert.throws(()=>app.move(b.id,'PROCESSING',s.id,'newer'),e=>e.code==='FIFO_OVERRIDE_REQUIRED');assert.equal(app.move(a.id,'PROCESSING',s.id,'older').fifoRank,1)});
