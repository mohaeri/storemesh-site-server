import test from'node:test';import assert from'node:assert/strict';import{StoreMesh}from'../src/domain.js';const setup=()=>{const app=new StoreMesh(),session=app.openSession('op'),batch=app.receive({sessionId:session.id,supplier:'S',product:'T',grade:'A',size:'L',weightKg:5},'r');return{app,session,batch}};
test('inventory adjustments preserve before and after values and never go negative',()=>{const{app,session,batch}=setup(),a=app.adjustInventory({batchId:batch.id,deltaKg:-1,reasonCode:'SHRINKAGE',reason:'Cold storage loss',userId:'manager',sessionId:session.id},'a');assert.deepEqual([a.beforeKg,a.deltaKg,a.afterKg],[5,-1,4]);assert.equal(app.state.inventoryAdjustments.length,1);assert.throws(()=>app.adjustInventory({batchId:batch.id,deltaKg:-5,reason:'bad'},'bad'),e=>e.code==='INSUFFICIENT_INVENTORY')});
test('waste and quarantine release have explicit audited transitions',()=>{const{app,session,batch}=setup();app.qualityCheck({batchId:batch.id,result:'QUARANTINED',inspectorId:'qc'},'q');assert.equal(batch.zone,'QUARANTINE');app.releaseQuarantine({batchId:batch.id,reason:'Retest passed',inspectorId:'manager',destinationZone:'COLD_ROOM'},'release');assert.equal(batch.zone,'COLD_ROOM');app.adjustInventory({batchId:batch.id,deltaKg:-5,reasonCode:'WASTE',reason:'Spoiled',userId:'manager',sessionId:session.id},'waste');assert.equal(batch.zone,'WASTE');assert.equal(batch.status,'WASTED')});
