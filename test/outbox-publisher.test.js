import test from 'node:test';import assert from 'node:assert/strict';import{StoreMesh}from'../src/domain.js';import{OutboxPublisher}from'../src/outbox-publisher.js';
test('outbox is ordered and acknowledged only after cloud success',async()=>{const app=new StoreMesh({clock:(()=>{let n=0;return()=>`2026-01-01T00:00:0${n++}Z`})()});app.openSession('op');const calls=[];const publisher=new OutboxPublisher({app,cloudUrl:'http://cloud',siteKey:'key',fetchImpl:async(url,options)=>{calls.push(JSON.parse(options.body));return{ok:true,json:async()=>({accepted:1,duplicates:0})}}});await publisher.flushOnce();assert.equal(calls[0].items[0].type,'SESSION_OPENED');assert.equal(app.state.outbox[0].status,'DELIVERED')});
test('failed cloud response leaves events pending for retry',async()=>{const app=new StoreMesh();app.openSession('op');const publisher=new OutboxPublisher({app,cloudUrl:'http://cloud',siteKey:'key',fetchImpl:async()=>({ok:false,status:503})});await assert.rejects(()=>publisher.flushOnce());assert.equal(app.state.outbox[0].status,'PENDING')});
